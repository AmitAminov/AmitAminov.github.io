Single-page personal portfolio site for Amit Aminov (static HTML/CSS, no build step, no external requests) — currently a draft for review, not published.
To deploy when approved: push this directory to a GitHub repo, then Settings → Pages → Deploy from branch (root); the `.nojekyll` file is already in place.
