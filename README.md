Single-page personal portfolio site for Amit Aminov (static HTML/CSS, no build step, no external requests).

**Live at https://amitaminov.github.io/** — deployed via GitHub Pages from the
`AmitAminov/AmitAminov.github.io` repo (branch `main`, root; `.nojekyll` in place).
To redeploy, push to `main`; Pages rebuilds automatically.
